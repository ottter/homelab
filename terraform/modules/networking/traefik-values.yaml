globalArguments:
  - "--global.sendAnonymousUsage=false"

service:
  type: LoadBalancer
  annotations:
    metallb.universe.tf/loadBalancerIPs: "${traefik_lb_ip}"

ports:
  web:
    redirectTo:
      port: websecure
  websecure:
    tls:
      enabled: true
