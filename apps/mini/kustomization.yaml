---
# Apps enabled on the 'mini' cluster.
# Porting an app from Terraform: add it to apps/base/<name>/ then list it here.
# This replaces Terraform's enable_<app> variables.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources: []
