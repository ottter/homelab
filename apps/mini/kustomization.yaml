---
# Apps enabled on the 'mini' cluster.
# Adding an app here replaces Terraform's enable_<app> variable.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base/_repos
  - ../base/overseerr
