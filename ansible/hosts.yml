---
all:
  hosts:
    minipc:
      ansible_host:                 192.168.0.210
      ansible_user:                 james
      ansible_ssh_private_key_file: ~/.ssh/ansible-mini
      ansible_become:               true
      ansible_become_pass:          "{{ lookup('env', 'HOMELAB_PASSWORD') }}"
