---
# Chart versions and network config moved out of this role:
#   versions, pool, IPs -> clusters/mini/cluster-vars.yaml, applied by Flux
#
# Only the Helm CLI toggle remains. Helm is not required — Flux's helm-controller
# runs in-cluster — so this is purely for manual inspection on the server.
networking_install_helm: true
