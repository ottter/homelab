name: Secret Scan

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  trufflehog:
    name: TruffleHog
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          # Full history: a secret removed in a later commit is still
          # retrievable from the earlier one, so scanning only the tip
          # would miss exactly the case that matters.
          fetch-depth: 0

      - name: Scan for verified secrets
        uses: trufflesecurity/trufflehog@main
        with:
          # The action passes --fail itself; repeating it here is an error.
          # --results=verified limits failures to credentials confirmed live
          # against the provider, so SOPS ciphertext does not trip it.
          extra_args: --results=verified
